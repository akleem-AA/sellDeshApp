import { StyleSheet } from "react-native";

const CustomStyle = StyleSheet.create({
    constainer: {
        flex: 1,
    },
    text: {
        color: 'black',
        fontSize: 18,
        fontWeight: '700',
        padding: 10
    }
});

export default CustomStyle;
